import { mkdirSync, writeFileSync, readFileSync, existsSync } from "fs";
import * as prettier from "prettier";
import { IMPKConfig } from "../index.d";

export default async function pretty(config: IMPKConfig) {}
